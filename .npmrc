# Enable legacy peer dependencies to avoid conflicts
legacy-peer-deps=true

# Use npm registry
registry=https://registry.npmjs.org/

# Set strict SSL
strict-ssl=true

# Force clean installation
force=true

# Logging level
loglevel=warn

# Save exact versions
save-exact=true